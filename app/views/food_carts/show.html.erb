<div id="food-cart-info">
  <%= render 'show_info' %>
</div>

<%= link_to 'Edit this Entry', edit_food_cart_path(@food_cart), remote: true %>
<%= link_to 'Delete this Entry', food_cart_path(@food_cart), data: {alert: 'Are you sure?', method: 'delete'} %>

<%= render 'modal' %>
