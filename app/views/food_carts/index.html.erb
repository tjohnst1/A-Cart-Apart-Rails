<div class="row">
  <div class="col-sm-8">
    <div id="map"></div>
  </div>
  <main class="col-sm-4">
    <ul id="cart-list">
      <% @food_carts.each do |cart| %>
        <li><%= link_to cart.name, food_cart_path(cart) %></li>
      <% end %>
    </ul>
  </main>
</div>
<%= render 'modal' %>
