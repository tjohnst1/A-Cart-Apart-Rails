<nav class="navbar navbar-default tk-rucksack">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#collapsing-navbar" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "PDX Food Cart Review", food_carts_path, class: "navbar-brand" %>
    </div>
    <div class="collapse navbar-collapse" id="collapsing-navbar">
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to 'Categories', tags_path %></li>
        <% if current_user %>
          <li><%= link_to 'Add a Food Cart', new_food_cart_path, remote: true, id: 'add-food-cart' %></li>
          <li><%= link_to 'Sign Out', destroy_user_session_path, method: 'delete' %></li>
        <% else %>
          <li>
            <%= link_to new_user_registration_path do %>
              <span class="glyphicon glyphicon-user"></span> Sign Up
            <% end %>
          </li>
          <li>
            <%= link_to new_user_session_path do %>
              <span class="glyphicon glyphicon-log-in"></span> Sign In
            <% end %>
          </li>
        <% end %>
      </ul>
      <%= form_tag(food_carts_path, method: :get, class:"navbar-form navbar-right") do %>
      <div class="form-group">
        <%= text_field_tag :query, params[:query], class:"form-control", placeholder:"Search" %>
      </div>
      <%= submit_tag "Search", name: nil, class: 'btn btn-primary' %>
      <% end %>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
